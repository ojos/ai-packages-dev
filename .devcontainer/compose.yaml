services:
  app:
    image: mcr.microsoft.com/devcontainers/base:ubuntu
    volumes:
      - ..:/workspaces/ojos-ai-packages-dev:cached
      # docker-outside-of-docker feature 用（compose 利用時は feature 側の mounts が適用されないため明示）
      - /var/run/docker.sock:/var/run/docker-host.sock
      # 認証状態は named volume に残す。資格情報をホストから注入しないため、
      # コンテナ内でのログインが唯一の手段であり、rebuild で消えると実用に耐えない。
      - gh-storage:/home/vscode/.config/gh
      - claude-storage:/home/vscode/.claude
      - gemini-storage:/home/vscode/.gemini
    command: sleep infinity
volumes:
  gh-storage:
  claude-storage:
  gemini-storage:
