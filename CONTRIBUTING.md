# 貢献について

**このリポジトリは外部からの Pull Request を受け付けていません。** issue は歓迎します。

## なぜ PR を受け付けないか

このリポジトリには、**既定ブランチへ入るコミットの author を、許可した identity に限定する機構**があります（`.github/workflows/identity-guard.yml`）。

fork からの PR を squash マージすると、生成されるコミットの author は PR の作者になります。つまり取り込んだ時点で、許可していない identity が既定ブランチの履歴へ入ります。**この機構は、それを防ぐために置いてあります。**

そのため、外部からの PR は**内容の良し悪しとは無関係に**、この検査で落ちます。受け付けられない PR を作っていただくことになるので、あらかじめ明示しています。

## 代わりにできること

- **issue を立ててください。** 不具合の報告、改善の提案、質問はいずれも歓迎します
- 変更の案を具体的に書いていただければ、こちら側で取り込みます。**その際は issue に貢献者として記録します**
- 配布物（`ai-playbook` / DevContainer Bootstrap）をご自身の環境で改変して使うことは、LICENSE の範囲で自由です

## fork について

**fork を止める設定はありません**（公開リポジトリでは fork を禁止できません）。fork していただくこと自体に問題はありません。PR の形で戻していただいても取り込めない、という点だけご承知ください。

**fork からの PR ではワークフローが自動では走りません。** 承認制にしているため、チェックが実行されないまま保留されます。これは不具合ではありません。
