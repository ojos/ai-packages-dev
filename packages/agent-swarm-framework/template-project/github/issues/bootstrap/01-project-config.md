Title: Bootstrap: project config を確定する
Milestone: M0 Package Bootstrap
Labels: dev-env

- displayName / projectSlug を確定する
- executionMode / remoteProvider を確定する
- lineStrategy / orchestratorMode / stateBackend / mergePolicy を確定する
- overrides に反映する
